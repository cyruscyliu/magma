diff --git a/poppler/JBIG2Stream.cc b/poppler/JBIG2Stream.cc
index 0dffc111..35872893 100644
--- a/poppler/JBIG2Stream.cc
+++ b/poppler/JBIG2Stream.cc
@@ -809,12 +809,17 @@ void JBIG2Bitmap::combine(JBIG2Bitmap *bitmap, int x, int y, unsigned int combOp
     oneByte = x0 == ((x1 - 1) & ~7);
 
     for (yy = y0; yy < y1; ++yy) {
+#ifdef MAGMA_ENABLE_FIXES
         if (unlikely(checkedAdd(y, yy, &yyy))) {
             continue;
         }
         if (unlikely((yyy >= h) || (yyy < 0))) {
             continue;
         }
+#endif
+#ifdef MAGMA_ENABLE_CANARIES
+        MAGMA_LOG("%MAGMA_BUG%", MAGMA_OR(y + yy >= h, y + yy < 0));
+#endif
 
         // one byte per line -- need to mask both left and right side
         if (oneByte) {
